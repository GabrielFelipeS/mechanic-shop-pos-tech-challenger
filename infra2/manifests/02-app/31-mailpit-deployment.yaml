apiVersion: apps/v1
kind: Deployment
metadata:
  name: mailpit
  namespace: ${namespace}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mailpit
  template:
    metadata:
      labels:
        app: mailpit
    spec:
      containers:
        - name: mailpit
          image: axllent/mailpit:latest
          ports:
            - containerPort: 8025
            - containerPort: 1025
