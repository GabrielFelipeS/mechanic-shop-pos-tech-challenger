apiVersion: v1
kind: Service
metadata:
  name: mechanic-shop-backend
  namespace: ${namespace}
spec:
  type: NodePort
  selector:
    app: mechanic-shop-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: ${app_node_port}
