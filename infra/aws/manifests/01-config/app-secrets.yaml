apiVersion: v1
kind: Secret
metadata:
  name: mechanic-shop-app-secret
  namespace: mechanic-shop
type: Opaque
stringData:
  POSTGRES_DB: "mechanic-shop-db"
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "postgres"
  SPRING_DATASOURCE_PASSWORD: "postgres"
  SEED_DEFAULT_PASSWORD: "123456"
  SMTP_USERNAME: ""
  SMTP_PASSWORD: ""
  SMTP_ACTIVE: "false"
  SMTP_HOST: "mailpit"
  SMTP_PORT: "1025"
  # Injected by CI from the NEW_RELIC_LICENSE_KEY repository secret; never commit a real key.
  NEW_RELIC_LICENSE_KEY: ""
