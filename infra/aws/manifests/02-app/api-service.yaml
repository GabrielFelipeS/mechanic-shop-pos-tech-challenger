apiVersion: v1
kind: Service
metadata:
  name: mechanic-shop-backend
  namespace: mechanic-shop
spec:
  type: NodePort
  selector:
    app: mechanic-shop-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
